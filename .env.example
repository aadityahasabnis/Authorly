# Image Upload Configuration for Testing
# Choose your upload provider: 'cloudinary' or 's3'
VITE_UPLOAD_PROVIDER=cloudinary

# ====================
# CLOUDINARY CONFIGURATION
# ====================
# Get your credentials from https://cloudinary.com (free tier available)

# Your Cloudinary cloud name (found in dashboard)
VITE_CLOUDINARY_CLOUD_NAME=your-cloud-name

# Create an unsigned upload preset in Cloudinary Settings > Upload
# Set it to "unsigned" mode for client-side uploads
VITE_CLOUDINARY_UPLOAD_PRESET=your-upload-preset

# Optional: folder name for uploaded images (default: authorly-test)
VITE_CLOUDINARY_FOLDER=authorly-test

# ====================
# AWS S3 CONFIGURATION
# ====================
# Get your credentials from AWS Console > IAM
# Make sure your backend API is running to generate presigned URLs

# AWS Region (e.g., us-east-1, eu-west-1)
VITE_AWS_REGION=us-east-1

# S3 Bucket name
VITE_AWS_BUCKET=my-authorly-images

# AWS Access credentials (keep these secret!)
VITE_AWS_ACCESS_KEY_ID=AKIA...
VITE_AWS_SECRET_ACCESS_KEY=...

# Optional: folder prefix in S3 bucket
VITE_AWS_PREFIX=authorly-test

# Optional: custom backend API endpoint for presigned URLs
# Default: /api/s3/presigned-url
VITE_AWS_API_ENDPOINT=/api/s3/presigned-url

# Optional: CloudFront distribution domain for CDN
# Example: d111111abcdef8.cloudfront.net
VITE_AWS_CLOUDFRONT_DOMAIN=

# ====================
# GENERAL SETTINGS
# ====================
# Maximum file size in MB (applies to both providers)
VITE_MAX_UPLOAD_SIZE_MB=10
